# Desktop UI framework. Required here even though release-flet.yml's own
# outer `pip install -r requirements.txt "flet[all]"` step also installs it -
# that install is for the CI runner building the app, not what ships inside
# it. `flet build`'s embedded-Python packaging step installs only from this
# file (or [project.dependencies] in pyproject.toml, which this repo doesn't
# set) into the bundle, so if flet isn't listed here, the packaged app itself
# has no flet in it and crashes with "ModuleNotFoundError: No module named
# 'flet'" the moment it starts (import flet as ft in desktop_app.py).
flet[all]

# Image Generation (checkpoint metadata; actual generation runs via ComfyUI)
safetensors>=0.3.0

# Utilities
pyyaml>=6.0
requests>=2.28.0
beautifulsoup4>=4.12.0

# Theming and self-update (see gerp93/KVG_Standards)
visual-assault-flet @ git+https://github.com/gerp93/VisualAssault.git@v0.2.0#subdirectory=packages/flet
kvg-updater @ git+https://github.com/gerp93/KVG_Standards.git@main#subdirectory=packages/python/kvg_updater

# Development tools
pytest>=7.3.0
black>=23.3.0
flake8>=6.0.0
