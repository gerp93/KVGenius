~~-the sliders on image gen and chat tabs need labels / units~~ ✅ (already had labels: "{value} steps", "CFG {value}", "{value}px", "Temp {value}", "{value} tokens")
~~-I want to show which GPU we are hooked up to at the top under the app title~~ ✅
~~-widen out the theme and model dropdowns so that the full text is visible for both~~ ✅ (theme=220px, model=450px)
~~-we need to add some ui controls around disabling certain things when certain types of models are loaded.  those are:~~
 ~~1) if an image model is not loaded there needs to be some kind of prominent banner on the generate tab stating that image model is loaded and the generate button should be disabled~~ ✅
 ~~2) on image gen also figure out the appropriate times for the stop button to be enabled, disabled~~ ✅ (already correct - enabled during generation, disabled otherwise)
 ~~3) if a chat model is not loaded there needs to be some kind of prominent banner on the chat tab stating that image model is loaded and the send button should be disabled~~ ✅
 - we meed to port over all the LoRA and model manager stuff from the gradio app
 ~~- the chat bubbles in the chat should change color with the theme~~ ✅ (using Colors.PRIMARY with opacity)
 ~~- can we somehow add spellchecking (just show a red underline for misspeelled words -- don't spend a lot of effort on this but add if it's something easy to add in flet)~~ ❌ Not available in Flet for desktop
 ~~- need buttons on image gen to save current prompt to prompt library along with all settings~~ ✅ (bookmark icon in settings panel)
 - need a better progress indicator for image gen
 - need better loading progress bars in general...more in your face...be sure to use the themes for all of them
 ~~- the overall quality of the images generated seems worse on flet than it was on gradio....don't do anything yet in terms of code but find out if there is any difference in how the generation is being done than we are doing in gradio that would cause that.  if there is a difference, add items to this todo list~~ ✅ Fixed: Added SDXL Lightning CFG=0 handling to core/image_gen.py (was missing)
 ~~- need more gallery options.  have a checkbox on each image for bulk deletes.  also have a second view option where all the images are shown in some soft of grid larger than they are in teh list. that view should be infiinite scroll and add more the further you scroll.  the list view we have currently should be paginated.~~ ✅ Gallery now has BOTH views: List view (paginated, 20/page with checkboxes) + Grid view (all images with scroll) - toggle with icons in header
 ~~- filter the LoRA dropdown based on what model we have loaded (i.e. only show SD loras if we have an SD model loaded)~~ ⚠️ Implemented but needs testing - added debug logging
 ~~- when we switch to another model in the dropdown we should just be able to click load and not have to click unload first.  unload should only be there if the model selected is the one loaded~~ ✅
 ~~- once the model is loaded the status indicator color in the dropdown itself does not change to green unless you re-select that model again~~ ✅
 ~~- cross off everything you complete on the to do.~~ ✅ 