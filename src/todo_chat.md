1) ~~when a prior chat is loaded from history, also set the character and persona that chat used.  and also load the model it used as well (give a warning if we need to unload another model first and give a warning if the char or persona or model no longer exists)  warning should all be pop ups.   if they don't exist just set them to the default~~ ✅ DONE
2) ~~on the chat history show the persona and char for each conversation in the history.~~ ✅ DONE
3) ~~have a delete all option for the chat history tab~~ ✅ DONE
4) ~~the clear conversation on the chat tab should actually delete the chat not just clear it.  it should say delte and not clear on the and should unlock the perona and char selecters again~~ ✅ DONE
5) ~~once the chat starts we need to lock in the char and persona.  disable the dropdowns for selecting those if the active chat has any messages.~~ ✅ DONE
6) ~~the "new chat" button has two plus icons...just have one~~ ✅ DONE
7) ~~export conversation should let user select where it is exported to.  default should be download folder on file system~~ ✅ DONE
8) ~~layout-wise, there is a huge gap between the search converstations box and teh list of prior convos.  do not have a big gap there~~ ✅ DONE
9) we need to implement some type of "memory" system.  could we have just an ordered list of memories that are key memories for the chat character that are somehow added to the context so if the chat gets real long they will remember? not a full chat history but just a key list of what the character remembers?  should be added to the convo history export. user can open a pop up on the UI to see the memories and also manually edit, add, delete them. they are on the conversation level.  if this seems difficult to implement or does not make sense don't do anything and ask me about it first for clarification
10) World Building: we need a system where we can build out world building contexts for certain characters that is not directly part of the character personality/system prompt.  the concept would involve defining an overall world building context (need some easier shorter term for this that you should come up with).  under the world building context we can enter a subtopic name, tags, and context.  one wb context to many subtopics relationship.  one to many wb contexts can be tied to any character.  when in a chat with a character, if tags for any subtopics match any words in the chat the conversation should tap into that subtopic and be aware of the information in the context. should be exportable as json.  this is a new tab under the chat tab.  if this seems difficult to implement or does not make sense don't do anything and ask me about it first for clarification
11) ~~Clone characters and personas (should add '- Clone' on the new one and then we can just edit like normal)~~ ✅ DONE
12) ~~can export characters and personas as JSON just like the convos are now~~ ✅ DONE
13) need a more standard system for interacting with the chat.  like text between two stars *like this* should indidcate action or internal thoughs rather than dialog (basically just narration rather than dialog).  and we should have a second text box in an expandable div on the chat box for giving direction along with the user's response.  this will not be read as dialog to the character, but will be directions telling the character what to do or how they should respond.  if this seems difficult to implement or does not make sense don't do anything and ask me about it first for clarification.  
